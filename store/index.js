import Vuex from "vuex";
const createStore = () => {
  return new Vuex.Store({
    state: {
      data: "demo"
    },
    mutations: {},
    actions: {},
    modules: {}
  });
};

export default createStore;
